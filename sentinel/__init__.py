"""GPU Cluster Sentinel — prediction + agent + learning backend.

See PRD.md / DESIGN.md / TASKS.md at the repo root for the full spec.
This package implements the data → telemetry → prediction → agent →
decision-log → learning loop (FR-2/3/5/8/9/11 in particular).
"""
